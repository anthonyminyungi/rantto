/**
 * 반복 사용되는 Tailwind 클래스 조합 상수
 */

/** 카드 컨테이너 기본 스타일 (DrawItem, SavedItem, ToastItem 등) */
export const CARD_STYLES =
  "bg-white dark:bg-neutral-900 border border-gray-200 dark:border-neutral-800 rounded-lg shadow-sm";

/** 모달 패널 기본 스타일 (AlertModal, ConfirmModal, ManualSelectModal) */
export const MODAL_PANEL_STYLES =
  "bg-white dark:bg-neutral-900 border border-gray-200 dark:border-neutral-700 shadow-lg dark:shadow-neutral-900/50 rounded-xl text-neutral-900 dark:text-neutral-100";

/** 아이콘 기본 사이즈 (ButtonGroup 내부 등) */
export const ICON_SIZE = "w-6 h-6 max-sm:w-5 max-sm:h-5";

/** Dropdown 내부 아이콘 사이즈 */
export const ICON_SIZE_SM = "w-5 h-5";
